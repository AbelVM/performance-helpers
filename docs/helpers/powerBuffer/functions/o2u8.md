[**performance-helpers**](../../../README.md)

***

[performance-helpers](../../../README.md) / [helpers/powerBuffer](../README.md) / o2u8

# Function: o2u8()

> **o2u8**(`obj`): `Uint8Array`\<`ArrayBufferLike`\>

Convert a value or buffer-like input to a UTF-8 encoded Uint8Array.

- If `obj` is already a `Uint8Array` it is returned as-is.
- If `obj` is an ArrayBuffer or TypedArray a zero-copy view is returned.
- Otherwise `JSON.stringify(obj)` is encoded as UTF-8.

## Parameters

### obj

`any`

Plain object or buffer-like (ArrayBuffer, TypedArray, Buffer).

## Returns

`Uint8Array`\<`ArrayBufferLike`\>

UTF-8 encoded view suitable for postMessage transfer.

## Throws

When no encoder (TextEncoder/Buffer) is available.

## Example

```ts
const u8 = o2u8({ hello: 'world' })
// use u8.buffer as transferable
```
